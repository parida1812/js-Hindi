// Remove extra spaces from both ends of a string.
str = "   lo p lo op   "
newStr = str.trim();
console.log(`The old string before removing the extra spaces from the string ${str}`);
console.log(`The new string after removing the extra spaces ${newStr}`);

